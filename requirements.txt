# Telegram-бот (управление из TG)
aiogram>=3.4

# Обход Cloudflare через TLS-фингерпринт браузера.
# Если не встанет — код сам откатится на requests (может ловить 403).
curl_cffi>=0.6.0

# Понадобится только на Этапе 2 (авто-сессия для 24/7 без ручного токена):
# pyrogram>=2.0
# tgcrypto
