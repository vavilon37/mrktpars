# Telegram-бот (управление из TG)
aiogram>=3.4

# HTTP к API MRKT. С браузерными заголовками обычный requests проходит
# (маркет троттлит только питоновский User-Agent) — этого достаточно.
requests>=2.31

# Необязательно: TLS-имитация браузера (ещё устойчивее к анти-боту).
# Если стоит — код использует его автоматически; если нет — берётся requests.
curl_cffi>=0.6.0

# Понадобится только на Этапе 2 (авто-сессия для 24/7 без ручного токена):
# pyrogram>=2.0
# tgcrypto
