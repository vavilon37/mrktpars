# Telegram-бот (управление из TG)
aiogram>=3.4

# HTTP к API MRKT. С браузерными заголовками обычного requests достаточно
# (маркет троттлит только питоновский User-Agent) — этого хватает.
requests>=2.31

# curl_cffi НАМЕРЕННО не включён: тяжёлая C-зависимость (libcurl), ломает
# сборку Docker на bothost, и не нужна — browser-заголовки решают 429.
# Код сам подхватит curl_cffi, если он вдруг окажется установлен.

# Понадобится только на Этапе 2 (авто-сессия 24/7 без ручного токена):
# pyrogram>=2.0
# tgcrypto
